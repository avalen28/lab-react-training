const FaceBook = ({ profiles }) => {
  console.log(profiles);
  return (
    <div className="container">
      {profiles.map{elem => console.log("hola")}}
    </div>
  );
};

export default FaceBook;
